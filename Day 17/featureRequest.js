// Feature Request:

// Linked List Script: Write a script that implements a linked list with methods to add, remove, and display nodes.

const linkedList = new linkedList();
linkedList.add(1);
linkedList.add(2);
linkedList.add(3);
linkedList.display();
console.log(linkedList.remove());
linkedList.display();

// Stack Script: Create a script that implements a stack and uses it to reverse a string.

const stack = new stack();
stack.push(1);
stack.push(2);
stack.push(3);
console.log(stack.peek());
console.log(stack.pop());
console.log(stack.pop());
console.log(stack.peek());
console.log(reverseString("world"));

// Queue Script: Write a script that implements a queue and simulates a printer queue.

const queue = new queue();
queue.enqueue(1);
queue.enqueue(2);
queue.enqueue(3);
console.log(queue.front());
console.log(queue.dequeue());
console.log(queue.dequeue());
console.log(queue.front());
printerQueue(["JobA", "JobB", "JobC"]);

// Binary Tree Script: Create a script that implements a binary tree with insertion and in-order traversal methods.

const binaryTree = new binaryTree();
binaryTree.insert(5);
binaryTree.insert(3);
binaryTree.insert(7);
binaryTree.insert(1);
binaryTree.insert(4);
binaryTree.inOrder(binaryTree.root);

// Graph Script: Write a script that implements a graph and performs breadth-first search (optional).

const graphScript = new graph();
graphScript.addVertex('X');
graphScript.addVertex('Y');
graphScript.addVertex('Z');
graphScript.addEdge('X', 'Y');
graphScript.addEdge('X', 'Z');
graphScript.bfs('X');

// Day 17 Completed...